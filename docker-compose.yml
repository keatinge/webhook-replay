version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "./replay.db:/whreplay/replay.db"
    deploy:
      restart_policy:
        condition: always
        delay: 5s
        max_attempts: 5
        window: 10s
